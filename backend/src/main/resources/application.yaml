spring:
  application:
    name: backend

  datasource:
    url: jdbc:postgresql://localhost:5432/portfolio_db
    username: postgres
    password: Hazari@786#

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  jwt:
    secret: ${JWT_SECRET:c3ByaW5nLWp3dC1zZWNyZXQtMzItYnl0ZS1rZXktMTIzNDU2Nzg5MA==}
    access:
      expiration: ${ACCESS_EXPIRATION:900000}
    refresh:
      expiration: ${REFRESH_EXPIRATION:1209600000}
